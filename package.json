{
  "name": "weather_app",
  "version": "1.0.0",
  "description": "Enter a location and get the weather (if no location is entered it takes your current location)",
  "main": "weather_app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dhaval Khatri",
  "license": "ISC",
  "dependencies": {
    "node-cmd": "^3.0.0",
    "yargs": "^11.0.0"
  }
}
